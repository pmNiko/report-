// Add the dialog title
$('#dialog h3').html("");

// Render the new form
$('.modal-body').html('<%= j render("show") %>');

var user = '<%= escape_javascript current_user.has_role? :admin %>';

// Show the dynamic dialog
$('#dialog').modal("show");

if(user){
  $('.container.text-center').css('visibility', 'hidden');
  alert('hola')
}else{
  $('.card-home-technicial').css('visibility', 'hidden');
  alert('chau')
}

$('#dialog').on('shown.bs.modal', function () {
})


$('#dialog').on('hidden.bs.modal', function () {
  if(user){
    $('.container.text-center').css('visibility', 'visible');
    alert('hola')
  }else{
    $('.card-home-technicial').css('visibility', 'visible');
    alert('chau')
  }
});
