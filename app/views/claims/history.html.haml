.container.text-center.container-history
  .card-group
    .card.border-info
      .card-header.text-center
        .row
          .col-4
          .col-4
            %h6= "HISTORY OF  " + "#{@tickets.first.client}"
          .col-4
            .float-right
              = link_to :back do
                = fa_icon "arrow-left", class: "fa-lg"
      - @tickets.each do |ticket|
        .card-body.text-center
          .card-header
            =ticket.date
            %br
            = ticket_resposables(ticket)
          .card-body.p-0
            %table.table.table-striped
              %tbody
                %tr
                  %td Ticket
                  %td Type
                  %td Concluded
                  %td Action
                %tr
                  %td #{ticket.number}
                  %td #{ticket.kind}
                  %td #{ticket.status}
                  %td
                    = link_to ticket_path(ticket), :remote => true do
                      = fa_icon "eye", right: true, class: "fa-lg"
          .card-body.p-0
            %table.table.table-hover.mb-0
              %thead
              %tbody
                %tr.row.m-0
                  %td.d-inline-block.col-12 #{ticket.observation}

= render 'dialog'
