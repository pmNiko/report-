= render 'dialog'
.container.text-center
  .row
    .col-4
    .col-8
      = render 'clock'
  .new-team#teams
    %tbody
      - if !@dreport.nil?
        = render @dreport
      - elsif !@team.nil?
        = render 'technician_team'
      - else
        
