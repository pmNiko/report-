= render 'dialog'
.container.text-center
  = render 'clock'
  .new-team#teams
    %tbody
      - if !@dreport.nil?
        = render '/dreports/show'
      - elsif !@team.nil?
        = render 'technician_team'
      - else
