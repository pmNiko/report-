// Add the dialog title
$('#dialog h3').html("<i class='glyphicon glyphicon-plus'></i> Crear");

// Render the new new
$('.modal-body').html('<%= j render("new") %>');

// Show the dynamic dialog
$('#dialog').modal("show");

// Set focus to the first element
$('#dialog').on('shown.bs.modal', function () {
    $('.first_input').focus()
  });
